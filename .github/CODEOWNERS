# Default reviewers: the @nexus-xyz/eng team. Any member can review and unblock --
# no single-person bottleneck.
#
# Deliberately a team rather than named individuals: membership changes in org
# settings, so joiners and leavers need no PR here. This file previously named a
# since-departed SDK lead (ENG-14958). GitHub silently omits a non-member from a
# review request, so such a line looks like coverage while providing none -- it
# never fails, it just quietly means nothing.
# Docs: https://docs.github.com/articles/about-code-owners
* @nexus-xyz/eng
